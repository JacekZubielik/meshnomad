name: meshnomad
description: "MeshNomad — independent Flutter client for MeshCore-compatible LoRa mesh devices"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.15.0+21

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^2.1.0
  # Vendored fork of jpnurmi/flutter_libserialport 0.6.0, patched to fix an
  # upstream libserialport bug: sp_new_config() never initializes
  # config->xon_xoff, so a freshly-allocated SerialPortConfig can carry
  # uninitialized heap garbage there and get rejected by sp_set_config()
  # with a spurious "Invalid XON/XOFF setting" (EINVAL). Also hardens
  # sp_open()/sp_free_port() to close the fd on open-time failures instead
  # of leaking it (which otherwise permanently EBUSYs the port for the life
  # of the process). See third_party/flutter_libserialport/.
  flutter_libserialport:
    path: third_party/flutter_libserialport
  provider: ^6.1.5+1
  shared_preferences: ^2.2.2
  uuid: ^4.3.3
  flutter_map: ^8.2.2
  latlong2: ^0.10.1
  fl_chart: ^1.0.0
  flutter_local_notifications: ^22.0.0
  crypto: ^3.0.3
  pointycastle: ^4.0.0
  http: ^1.2.0
  cached_network_image: ^4.0.0
  flutter_cache_manager: ^3.4.1
  flutter_foreground_task: ^11.0.0
  characters: ^1.4.0
  package_info_plus: ^10.1.0
  mobile_scanner: ^7.1.4  # QR/barcode scanning
  qr_flutter: ^4.1.0      # QR code generation
  file_picker: ^12.0.0    # Local .bin firmware file selection (Flasher)
  url_launcher: ^6.3.0    # Launch URLs in system browser
  flutter_linkify: ^6.0.0 # Auto-detect and linkify URLs in text
  gpx: ^2.3.0
  path_provider: ^2.1.5
  share_plus: ^13.1.0
  build_pipe: ^0.3.1
  material_symbols_icons: ^4.2928.1
  web: ^1.1.1
  flutter_svg: ^2.0.10+1
  flutter_blue_plus_platform_interface: ^9.0.2
  ml_algo: ^16.0.0
  ml_dataframe: ^1.0.0
  llamadart: ^0.8.0
  flutter_langdetect: ^0.0.2
  cryptography: ^2.9.0
  cryptography_flutter: ^2.3.4
  convert: ^3.1.2
  flutter_skill: ^0.9.36
  flutter_secure_storage: ^11.0.0

hooks:
  user_defines:
    llamadart:
      llamadart_native_backends:
        platforms:
          android-arm64:
            backends: [cpu]
          android-x64:
            backends: [cpu]

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  alchemist: ^0.14.0
  integration_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/
    - assets/flasher/catalog.json

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter.ttf
        - asset: assets/fonts/InterItalic.ttf
          style: italic
    - family: JetBrains Mono
      fonts:
        - asset: assets/fonts/JetBrainsMono.ttf
        - asset: assets/fonts/JetBrainsMono-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/JetBrainsMono-Bold.ttf
          weight: 700

flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
  windows:
    generate: true
  macos:
    generate: true
  image_path: "assets/icons/app_icon_1024.png"
  remove_alpha_ios: true
  adaptive_icon_background: "#3F6212"
  adaptive_icon_foreground: "assets/icons/app_icon_foreground_1024.png"
  adaptive_icon_monochrome: "assets/icons/app_icon_foreground_1024.png"

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package



build_pipe:
  workflows:
    default:
      clean_flutter: true          # Optional: Cleans old build artifacts before running
      generate_log: true           # Optional: Outputs a build log file for debugging
      platforms:
        web:
          build:
            build_command: flutter build web --release --pwa-strategy=none
            # Strongly recommended: disables the default service worker which often causes more cache headaches
            add_version_query_param: true
            # This is the key flag! It appends ?v=<your pubspec version> to bootstrap/JS files
