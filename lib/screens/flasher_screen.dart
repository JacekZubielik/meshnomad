import 'package:flutter/material.dart';

/// Placeholder — replaced by the real wizard in task 07 of the Flasher USB
/// implementation series (docs/superpowers/plans/2026-08-24-usb-flasher.md).
class FlasherScreen extends StatelessWidget {
  const FlasherScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('TODO')));
  }
}
